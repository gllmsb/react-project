import style from "./Main.module.scss"
export const Main = () => {
    return (
    <section className={style.mainStyle}>
        <h2 className={style.title}>This is my Main section</h2>
    </section>
    ) 
}