import style from "./Footer.module.scss"
export const Footer = () => {
    return (
    <footer className={style.footerStyle}>
        <h2 className={style.title}>This is my footer</h2>
    </footer>  
    )
}